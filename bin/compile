#!/bin/bash

soureFolder='src'

if [ "$(ls -A $soureFolder)" ]; then
	node pull.js
fi

cd src/theme

node_modules_folder="node_modules"

if [ ! -d "$node_modules_folder" ]; then
	npm install
fi

cd ../..

./node_modules/.bin/gulp -f src/theme/gulpfile.js compile
