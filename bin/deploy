#!/bin/bash
wpFolder="./.wp/wp-content"
distFolder="dist"

sudo chmod -R 777 $wpFolder

mkdir -p $wpFolder/themes/
mkdir -p $wpFolder/plugins/

if [ "$(ls A $distFolder)" ]; then
	npm run build "$@"
	rsync -r -u $distFolder/* $wpFolder
fi
